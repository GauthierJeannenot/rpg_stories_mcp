// Map generation is intentionally absent.
// The visual map lives in the React/Konva client (JPEG background + grid overlay).
// Use the `create_map` MCP tool to define a map's grid dimensions and metadata.
export {};
